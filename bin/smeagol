#!/usr/bin/env ruby

if ARGV.include?('-b') || ARGV.include?('--build')
  #if defined?(Daemons)
  #  raise 'cannot daemonize a static build'
  #end
  require 'smeagol/generator'
  Smeagol.generate(ARGV)
else
  require 'smeagol/server'
  Smeagol.serve(ARGV)
end

