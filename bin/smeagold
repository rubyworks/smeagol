#!/usr/bin/env ruby

#puts "Command `smeagold' is deprecated, use `smeagol-server' instead."

# TODO: Aren't the separate tools for daemonizing a process?
#       Do we really need to provide our own?
#
# TODO: If we do need this consider using the simpler Dente gem
#       instead.

require 'daemons'
Daemons.run_proc(File.basename(__FILE__)) do
  require 'smeagol'
  Smeagol::CLI.serve(argv)
end

